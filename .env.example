# Mission Control — Docker Compose environment
# Copy to .env and edit:  cp .env.example .env

# ── Required ─────────────────────────────────────────────────
MC_ADMIN_PASS=change-me

# ── Optional overrides ───────────────────────────────────────
# MC_ADMIN_USER=admin
# MC_POLL_MS=5000
# MC_BD_BIN_HOST=/home/zhugehyuk/.local/bin/bd
# MC_BEADS_DIR=./.beads
# MC_GATEWAY_URL=ws://host.docker.internal:18789
# MC_GATEWAY_TOKEN=
# MC_GATEWAY_PASSWORD=
# RUST_LOG=info
